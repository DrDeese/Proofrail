version: 1

claims:
  allowed-statuses:
    - verified

overall:
  allowed-verdicts:
    - partially_verified

exceptions:
  - claim-id: workflow-policy-wired
    allowed-statuses:
      - unsupported
